services:
  - type: web
    name: teloplay-api
    runtime: python
    rootDir: render-api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free