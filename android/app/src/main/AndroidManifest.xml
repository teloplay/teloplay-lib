<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- audio_service-এর জন্য প্রয়োজনীয় নতুন permissions -->
    <!-- Android 13+ (API 33+): notification দেখানোর জন্য runtime permission দরকার -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Android 14+ (API 34+): mediaPlayback foreground service type ঘোষণার জন্য -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <application
        android:label="TeloPlay"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ⚠️ নতুন যোগ করা — audio_service-এর মূল requirement।
             এই <service> ছাড়া AudioService.init() কল করলে
             "The Activity class declared in your AndroidManifest.xml
             is wrong or has not provided the correct FlutterEngine"
             crash আসে, কারণ প্ল্যাটফর্ম এই service খুঁজে না পেয়ে
             MediaBrowserService bind করতে ব্যর্থ হয়। -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>

        <!-- ⚠️ নতুন যোগ করা — হেডসেট/ব্লুটুথ বাটন (play/pause/next)
             থেকে media button event গ্রহণ করার জন্য audio_service-এর
             প্রয়োজনীয় receiver। -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>